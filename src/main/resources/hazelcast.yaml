hazelcast:
  cluster-name: hz_demo
  network:
    join:
      multicast:
        enabled: true
      tcp-ip:
        enabled: false
        interface: 127.0.0.1
        member-list:
          - 127.0.0.1
  map:
    default:
      time-to-live-seconds: 3600
